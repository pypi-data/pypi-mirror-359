[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ActPy"
version = "0.0.0"
description = "An empty placeholder Python package."
authors = [{ name = "Spece" }]
readme = "README.md"
requires-python = ">=3.8"

