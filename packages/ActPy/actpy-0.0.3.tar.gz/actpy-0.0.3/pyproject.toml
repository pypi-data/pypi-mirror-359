[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "ActPy"
version = "0.0.3"
description = "ActPy placeholder"
readme = "README.md"
authors = [{name="Spece"}]
requires-python = ">=3.8"
