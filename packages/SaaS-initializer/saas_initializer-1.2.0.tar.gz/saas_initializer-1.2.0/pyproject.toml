[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "SaaS-initializer"
version = "1.2.0"
description = "A SaaS initializer for web development using Django and React"
readme = "README.md"
license = "MIT"
authors = [
    { name = "Vervloessem Lucas", email = "lucas.vervloessem.atc@gmail.com" }
]
dependencies = [
    "questionary",
    "gitpython"
]
requires-python = ">=3.7"
classifiers = [
    "Programming Language :: Python :: 3",
    "Operating System :: OS Independent"
]

[project.scripts]
SaaS-init = "saas_initializer.initializer:main"