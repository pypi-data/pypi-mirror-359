[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "abstractart"
version = "0.2.0"
description = "Randomly generating abstract stickman"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[project.scripts]
abstractart = "random_stickman:main"
stickman = "stickman:main"
