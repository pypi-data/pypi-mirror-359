account_ids:
  - 012345678900

#aws_regions:  ## If aws_regions is not specified, the script will look at all regions
#  - us-east-1
#  - us-west-2

controller:
  ip: 1.1.1.1
  username: admin

#defaults:
#  add_vpc_cidr: True  ## Determine whether we should generate terraform code for adding VPC cidr.
#  hpe: True  ## Spoke gateway setting (True/False).
#  role_name: "aviatrix-role-app"  ## IAM role assumed to execute API calls. Default is aviatrix-role-app.
#  filter_cidrs: ["0.0.0.0/0"]  ## Filters out any route within specified CIDR when copying the route table.  No need to add RFC1918 routes in the list; they are filtered by default.
#  gw_zones: []  ## Zone letters to deploy spoke gateways in. Discovery will deduce the zones if an empty list [] is used.  Defaults to [].
#  managed_tgw: False  ## Defaults to False
#  spoke_gw_size: "t3.micro"  ## Spoke gateway instance size.
#  domain: "prod"  ## Default is None
#  encrypt: False  ## No volume encryption. Default is False.
#  inspection: False  ## No inpsection required. Default is False.
#  transit_gw_name: ""  ## This field is required if config.configure_gw_names is False.
