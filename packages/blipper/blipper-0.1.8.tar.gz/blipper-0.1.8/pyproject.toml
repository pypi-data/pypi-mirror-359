[project]
name = "blipper"
version = "0.1.8"
description = "Blipper is an AI application that empowers you to access large language model capabilities through a high-level interface. Blipper provides a lot of functions each with a task of their own. We call them Blips."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "requests>=2.32.3",
]
