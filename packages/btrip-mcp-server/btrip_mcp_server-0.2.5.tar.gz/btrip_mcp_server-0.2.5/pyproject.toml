[project]
name = "btrip-mcp-server"
version = "0.2.5"
description = "这是一个最新的版本.支持很多动作"
readme = "README.md"
authors = [
    { name = "洒花", email = "zhangwenzhe.zwz@alibaba-inc.com" }
]
requires-python = ">=3.10"
dependencies = [
    "httpx>=0.28.1",
    "httpx_sse>=0.4.0",
    "mcp[cli]>=1.8.1",
]

[project.scripts]
btrip-mcp-server = "btrip_mcp_server:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
