[project]
name = "chaoshen-mcp-demo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "liuchao11", email = "liuchao11@staff.weibo.com" }
]
requires-python = ">=3.13"
dependencies = [
    "mcp[cli]>=1.10.1",
]

[project.scripts]
chaoshen-mcp-demo = "chaoshen_mcp_demo:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
