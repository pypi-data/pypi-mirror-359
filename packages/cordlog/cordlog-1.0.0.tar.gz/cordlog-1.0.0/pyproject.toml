[tool.poetry]
name = "cordlog"
version = "1.0.0"  # Заменяется автоматически
description = "Colored logging formatter for Python"
authors = ["LordCode", "Dybfuo"]
readme = "README.md"
packages = [{ include = "cordlog" }]

[tool.poetry.dependencies]
python = "^3.11"
aiohttp = "^3.11.18"
requests = "^2.32.3"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
