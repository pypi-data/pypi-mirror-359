[build-system]
requires = ["setuptools >= 77.0.3"]
build-backend = "setuptools.build_meta"

[project]
name = "countonlyonechar"
version = "0.0.6"
authors = [
    { name = "Oleksandr Bohatyi", email = "alexbogplay@gmail.com" },
]
description = "Package for counting unique characters in a string"
readme = "README.md"
requires-python = ">=3.13"
classifiers = [
    "Programming Language :: Python :: 3.13",
    "Operating System :: OS Independent",
]
license = "MIT"
license-files = ["LICEN[CS]E*"]

[tool.hatch.build.targets.wheel]
packages = ["countonlyonechar"]

[tool.setuptools.packages.find]
where = ["."]

[project.scripts]
count-chars = "countonlyonechar.collect_framework:main"

[tool.hatch.build]
include = [
    "countonlyonechar",
    "tests",
    "README.md",
    "LICENSE"
]
