[project]
name = "demo-cli-nahuel"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Nahuel Defossé", email = "nahuel.deofsse@ibm.com" }
]
requires-python = ">=3.13"
dependencies = []

[project.scripts]
demo-cli = "demo_cli.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
