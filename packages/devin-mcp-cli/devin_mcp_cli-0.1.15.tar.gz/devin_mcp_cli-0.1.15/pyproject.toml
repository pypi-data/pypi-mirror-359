[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "devin-mcp-cli"
version = "0.1.15"
description = "A CLI for MCP"
requires-python = ">=3.11"
readme = "README.md"
authors = [
  { name = "Your Name", email = "fede@cognition.ai" }
]
keywords = ["cli", "mcp"]
dependencies = [
  "typer>=0.15.2",
  "rich>=13.9.4",
  "python-dotenv>=1.1.0",
  "asyncio>=3.4.3",
  "mcp>=1.6.0",
  "httpx-auth>=0.22.0",
]

[project.scripts]
mcp-cli = "mcp_cli.main:app"

[project.optional-dependencies]
wasm = []

[tool.setuptools]
package-dir = { "" = "src" }

# UV-specific configuration to ensure isolated environments
[tool.uv]
python-preference = "managed"  # Prefer uv-managed Python installations
python-downloads = "automatic"  # Allow automatic Python downloads

[dependency-groups]
dev = [
  "fastapi>=0.115.13",
  "pex>=2.40.1",
  "pytest>=8.3.5",
  "pytest-asyncio>=0.25.3",
  "pytest-cov>=4.0.0",
  "pytest-mock>=3.14.0",
  "uvicorn>=0.34.2",
]
