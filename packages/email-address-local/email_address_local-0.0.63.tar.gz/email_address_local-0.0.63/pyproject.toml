# This file should be in the future instead of setup.py
# https://python-poetry.org/docs/pyproject

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.poetry]
name = "email-address-local"
version = "0.0.56" # https://pypi.org/project/email-address-local i.e. https://pypi.org/project/storage-local/
description = "email address local Python Package"
readme = "README.md"
authors = [
    "Circlez.ai <info@circlez.ai>",
]