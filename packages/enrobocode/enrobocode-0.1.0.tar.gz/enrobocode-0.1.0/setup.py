from setuptools import setup, find_packages

setup(
    name="enrobocode",
    version="0.1.0",
    description="English Robot Code — simple commands to control robots",
    author="Your Name",
    packages=find_packages(),
    install_requires=[],
)
