# {{ plugin_type }} plugins

This is the list of `{{ plugin_type }}` plugins in the `{{ fqcn }}` collection:

| Plugin | Description |
| ------ | ------ |
{% for plugin, plugin_data in plugins.items() %}
| [{{ plugin }}](./{{ plugin | replace(fqcn + '.', '') }}.md) |
{{- plugin_data['doc']['short_description'] | default('--') }}
{% endfor %}
