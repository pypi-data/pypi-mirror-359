[project]
name = "my-demo-cli"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Nahuel Defossé", email = "nahuel.deofsse@ibm.com" }
]
requires-python = ">=3.10,<3.14"
dependencies = [
    "click>=8.2.1",
    "rich>=14.0.0",
]

[project.scripts]
demo-cli = "my_demo_cli.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
