[project]
name = "ode2tn"
version = "1.1.0"
description = "A Python package to turn arbitrary polynomial ODEs into a transcriptional network simulating it."
authors = [
    { name="Dave Doty", email="doty@ucdavis.edu" }
]
readme = "README.md"
requires-python = ">=3.10"
license = "MIT"
dependencies = [
    "gpac",
    "scipy>=1.15",
    "sympy>=1.13",
]

[project.urls]
Homepage = "https://github.com/UC-Davis-molecular-computing/ode2tn"
Issues = "https://github.com/UC-Davis-molecular-computing/ode2tn/issues"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["ode2tn"]
