[build-system]
requires = ["setuptools>=42.0"]
build-backend = "setuptools.build_meta"

[project]
name = "ormantism"
version = "0.1"
description = "A lightweight SQLite ORM built on Pydantic for simple CRUD operations with minimal code"
readme = "README.md"
requires-python = ">=3.12"
authors = [
    {name = "Mathieu Rodic"},
]
keywords = ["orm", "sqlite"]
license = {text = "MIT"}
classifiers = [
    "License :: OSI Approved :: MIT License",
]
dependencies = [
    "pydantic>=2.11.7",
    "pydantic_core>=2.33.2",
]

[project.urls]
Homepage = "https://github.com/mathieurodic/ormantism"
