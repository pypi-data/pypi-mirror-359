[project]
name = "paladin-mcp-server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Jianqi Wang", email = "wangjianqi2009@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "mcp[cli]>=1.10.1",
]

[project.scripts]
paladin-mcp-server = "paladin_mcp_server:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
