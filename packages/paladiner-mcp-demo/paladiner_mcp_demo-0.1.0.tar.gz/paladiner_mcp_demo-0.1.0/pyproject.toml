[project]
name = "paladiner-mcp-demo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Jianqi Wang", email = "wangjianqi2009@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "mcp[cli]>=1.10.1",
]

[project.scripts]
paladiner-mcp-demo = "paladiner_mcp_demo:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
