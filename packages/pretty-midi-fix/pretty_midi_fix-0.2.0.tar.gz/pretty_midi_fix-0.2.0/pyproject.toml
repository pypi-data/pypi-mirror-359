[project]
name = "pretty-midi-fix"
version = "0.2.0"
description = "Updated Version of pretty-midi without fluidsynth"
dependencies = [
    "mido>=1.3.3",
    "numpy>=1.7.0",
    "six>=1.17.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
