[project]
name = "promptlibz"
version = "1.0.1"
description = "用来存储优质prompt的仓库"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [ "dill>=0.4.0", "llama-index>=0.12.31", "pyyaml>=6.0.2", "toml>=0.10.2",]

[tool.pytest.ini_options]
testpaths = [ "tests",]
pythonpath = [ "src",]
