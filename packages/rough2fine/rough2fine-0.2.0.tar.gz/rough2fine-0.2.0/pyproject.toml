[project]
name = "rough2fine"
version = "0.2.0"
description = "通过给定的资料，获得细分场"
authors = [
    {name = "GZT",email = "ave1age@163.com"}
]
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    'mcp>=1.9.4'
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[project.scripts]
rough2fine = "rough2fine.server:main"