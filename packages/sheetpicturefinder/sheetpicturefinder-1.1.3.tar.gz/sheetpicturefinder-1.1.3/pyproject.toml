[build-system]
requires = ["setuptools>=42,<69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "sheetpicturefinder"
version = "1.1.3"
description = "SheetPictureFinder is a pure Python utility for extracting images from Excel .xlsx files — specifically from worksheets and cell addresses. It parses the internal XML structure of Excel files and maps embedded images to their anchor locations."
readme = "README.md"
license = {text = "MIT"}
authors = [
  { name = "Samprit Sarkar", email = "samprits5@gmail.com" }
]
requires-python = ">=3.8,<3.10"
dependencies = [
  "Pillow==10.3.0"
]

[project.urls]
Homepage = "https://github.com/samprits5/"