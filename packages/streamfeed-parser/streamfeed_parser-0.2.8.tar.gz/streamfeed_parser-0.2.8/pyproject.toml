[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "streamfeed-parser"
version = "0.2.8"
description = "A lightweight streaming parser for CSV and XML feeds over HTTP/FTP with automatic compression handling."
authors = [{ name = "Hans-Christian Bøge Pedersen", email = "hello@devwithhans.com" }]
license = { file = "LICENSE" }
readme = "README.md"
requires-python = ">=3.7"
dependencies = [
    "requests>=2.25.0"
]

[project.urls]
Repository = "https://github.com/devwithhans/streamfeed-parser"
Developer = "https://devwithhans.com"

[tool.setuptools]
packages = ["streamfeed"]
