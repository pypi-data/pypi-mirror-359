[pytest]
# Ignore the package's __init__.py when collecting tests
norecursedirs = __init__.py
# Add the test directory to the Python path
pythonpath = .
# Disable warnings about import errors
filterwarnings =
    ignore::ImportWarning 