[tool.poetry]
name = "vllmctl"
version = "0.2.1"
description = "CLI tool for launching and managing vllm model servers via SSH and tmux"
authors = ["vllmctl <adefful46@gmail.com>"]
license = "MIT"
readme = "README.md"
packages = [{include = "vllmctl"}]

[tool.poetry.dependencies]
python = ">=3.8,<4.0"
typer = ">=0.9.0"
rich = ">=13.0.0"
psutil = ">=5.9.0"
requests = ">=2.28.0"

[tool.poetry.scripts]
vllmctl = "vllmctl.cli:app"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.group.dev.dependencies]
#pytest = ">=7.0.0"
#pytest-mock = ">=3.10.0" 